{
  "name": "formforge-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend-react install && npm --prefix frontend-react run build && cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
